# Node.js Core Dependency Trees
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Persistent Session Environment Secrets Locks
.env
.env.local
.env.production
.env.development

# System Workspace Caches
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project Progress Telemetry Backups
macprep_backup_*
*.log

# Local Claude-to-Codex migration exports. Raw transcripts may contain secrets,
# private business details, or customer/contact data; distill safe summaries into
# docs/codex/ instead of committing the raw exports.
local/claude-export/

# Large seed/ingestion payloads — the Supabase DB is the source of truth.
# Kept on disk for local seed scripts but not committed (regenerable artifacts).
data/questions.json
data_expansion_payload.json

# Dead Expo/React Native scaffolding (the app is vanilla HTML/JS via Express)
.expo/

# Private legal documents (never serve/commit)
MACPrep_LLC_Operating_Agreement.rtf
*_Operating_Agreement.rtf

# Local reference (email templates pasted into Supabase)
MACPrep_email_templates.html

# Sensitive business/legal/tax documents — never commit or web-serve
*.pdf
CP_575*
*.p12
*.pfx

# Native push credentials & config — Firebase/APNs. Live on Jake's machine or in
# Render/Firebase secret stores, NEVER in git.
mobile/android/app/google-services.json
mobile/ios/App/App/GoogleService-Info.plist
*firebase-adminsdk*.json
serviceAccount*.json
*.p8
AuthKey_*.p8

# Supabase CLI runtime state. Versioned migrations above this directory are source.
supabase/.temp/
