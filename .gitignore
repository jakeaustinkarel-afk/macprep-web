# Node.js Core Dependency Trees
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Persistent Session Environment Secrets Locks
.env
.env.local
.env.production
.env.development

# System Workspace Caches
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project Progress Telemetry Backups
macprep_backup_*
*.log

# Large seed/ingestion payloads — the Supabase DB is the source of truth.
# Kept on disk for local seed scripts but not committed (regenerable artifacts).
data/questions.json
data_expansion_payload.json

# Dead Expo/React Native scaffolding (the app is vanilla HTML/JS via Express)
.expo/
