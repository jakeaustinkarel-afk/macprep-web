{
  "name": "macprep-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "MACPrep native iOS/Android shell (Capacitor). Loads the live macprep.org web app so every web deploy auto-updates the phone apps — one codebase, no re-porting.",
  "scripts": {
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
