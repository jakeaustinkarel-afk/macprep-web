# Copy to keystore.properties locally, then replace every placeholder.
# Both this file and the referenced .jks must remain outside version control.
storeFile=macprep-release.jks
storePassword=replace-with-a-strong-password
keyAlias=macprep
keyPassword=replace-with-a-strong-password
